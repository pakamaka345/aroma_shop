<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t('Профіль') %></title>
  <%= stylesheet_link_tag 'profile.css' %>
  <%= javascript_include_tag 'application' %>
</head>
<body>
  <div class="profile-container">
    <h1><%= t('Профіль') %></h1>
    <% if current_user.admin? %>
      <h2><%= t('Адмін-панель') %></h2>
      <button onclick="window.location.href='/users/new'"><%= t('Додати нового користувача') %></button>
      <button onclick="window.location.href='/users'"><%= t('Керувати користувачами') %></button>
    <% end %>
    <h2><%= t('Історія замовлень') %></h2>
    <ul>
      <% current_user.orders.each do |order| %>
        <li>
          <%= t('Номер замовлення') %>: <%= order.id %>, 
          <%= t('Всього') %>: <%= order.total %>, 
          <%= t('Дата') %>: <%= order.created_at.to_date %>
        </li>
      <% end %>
    </ul>
    <button id="edit-profile-button"><%= t('Редагувати профіль') %></button>
    <div id="edit-profile-form" style="display: none;">
      <%= form_with model: current_user, url: user_path(current_user), method: :patch do |f| %>
        <%= f.label :first_name, t('Ім\'я') %>
        <%= f.text_field :first_name %>
        <%= f.label :last_name, t('Прізвище') %>
        <%= f.text_field :last_name %>
        <%= f.label :email, t('Email') %>
        <%= f.email_field :email %>
        <%= f.submit t('Зберегти зміни') %>
      <% end %>
    </div>
    <button onclick="window.location.href='/logout'"><%= t('Вийти') %></button>
  </div>
  <script>
    document.getElementById('edit-profile-button').addEventListener('click', function() {
      document.getElementById('edit-profile-form').style.display = 'block';
    });
  </script>
</body>
</html>
